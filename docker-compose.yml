services:
  maxmux:
    build: .
    container_name: maxmux
    restart: unless-stopped
    ports:
      - "4000:4000"
    volumes:
      - ./config.yaml:/config.yaml:ro
      - ./data:/data
    command: ["-config", "/config.yaml", "-data", "/data/maxmux.json"]
