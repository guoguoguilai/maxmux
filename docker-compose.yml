services:
  maxmux:
    image: pageguo/maxmux:latest
    restart: unless-stopped
    ports:
      - "${PORT:-4000}:4000"
    volumes:
      - ./config.yaml:/config.yaml:ro
      - ./data:/data
    command: ["-config", "/config.yaml", "-data", "/data/${COMPOSE_PROJECT_NAME:-maxmux}.db"]
